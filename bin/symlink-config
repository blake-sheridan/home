#!/bin/bash

root="$( cd "$( dirname "${BASH_SOURCE[0]}" )"/.. >/dev/null 2>&1 && pwd )"

ln -fs $root/etc/emacs.d ~/.emacs.d
ln -fs $root/etc/gitconfig ~/.gitconfig
ln -fs $root/etc/zshenv ~/.zshenv
ln -fs $root/etc/zshrc ~/.zshrc
